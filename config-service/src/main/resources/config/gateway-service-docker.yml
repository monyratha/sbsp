server:
  port: 8060

app:
  cors:
    origins: http://localhost:5666,http://192.168.80.128:5666,http://192.168.64.1:5666

eureka:
  client:
    serviceUrl:
      defaultZone: http://discovery-service:8061/eureka/

logging:
  pattern: 
    console: "%d{yyyy-MM-dd HH:mm:ss} [%X{traceId:-},%X{spanId:-}] ${LOG_LEVEL_PATTERN:-%5p} %m%n"
              
spring:
  output:
    ansi:
      enabled: always
  cloud:
    gateway:
      globalcors:
        add-to-simple-url-handler-mapping: true
      discovery:
        locator:
          enabled: true
      routes:
      - id: auth-service
        uri: lb://auth-service
        predicates:
          - Path=/auth/**
        filters:
          - RewritePath=/auth/v3/api-docs(?<path>.*), /v3/api-docs$\{path}
      - id: jwk
        uri: lb://auth-service
        predicates:
          - Path=/.well-known/openid-configuration
      - id: user-service
        uri: lb://user-service
        predicates:
        - Path=/user/**
        filters:
          - RewritePath=/user/v3/api-docs(?<path>.*), /v3/api-docs$\{path}
      - id: openapi
        uri: http://192.168.80.128:${server.port}
        predicates:
        - Path=/v3/api-docs/**
        filters:
        - RewritePath=/v3/api-docs/(?<path>.*), /$\{path}/v3/api-docs

springdoc:
  swagger-ui:
    urls:
      - name: auth
        url: /v3/api-docs/auth
      - name: user
        url: /v3/api-docs/user

management:
  tracing:
    sampling:
      probability: 1.0
  zipkin:
    tracing:
      endpoint: http://zipkin:9411/api/v2/spans
